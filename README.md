# GroceryInventory

Just a basic Grocery Inventory system
-allows us to keep track of items their quantity and location
-saves the information in a file

## Development Notes

Claude Code configuration files (`.claude/`, `CLAUDE.md`) are excluded from version control via `.gitignore`.
